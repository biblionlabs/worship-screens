export global Settings {
    in property <[string]> monitors;
    in-out property <int> selected-monitor;
}

export struct Bible {
    id: string,
    name: string,
    english-name: string,
    installed: bool,
    installing: bool,
    progress: float,
}

export struct Verse {
    bible: Bible,
    book: string,
    chapter: int,
    part: int,
    verse: { a: int, b: int},
    text: string,
}
